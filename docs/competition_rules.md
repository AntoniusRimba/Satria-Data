# Competition Rules

Rules and constraints for the Big Data Challenge (BDC) 2026.

## Major Rules
- **No external labeled datasets**: Do not download/inject other labeled datasets.
- **No data leakage**: Never train/tune on test set images.
- **Pixel-only features**: Use only image pixel values (no metadata).
